# Test and lint tooling. Deliberately not in requirements.txt: that file is
# what the AgentCore Runtime image installs, and a runtime image has no
# business carrying a test runner or a linter — it is weight to build,
# push and scan for code that never executes in production.
#
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.8.0
